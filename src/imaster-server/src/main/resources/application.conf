databaseConfig = "default"

default = {
  connectionPool = "HikariCP"
  driverClassName = "org.h2.Driver"
  url = "jdbc:h2:tcp://localhost:9092/~/imaster;IFEXISTS=TRUE"
  user = "root"
  password = "root"
  minPoolSize = 1
  acquireIncrement = 1
  maxPoolSize = 150
}